# ZDOTDIR is already exported (by this very file's own zshenv.zsh) by the time any later zsh
# starts, so zsh looks for .zshenv here instead of $HOME -- keep this in sync with ~/.zshenv.
. "${HOME}/.config/zsh/zshenv.zsh"
